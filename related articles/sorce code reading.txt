tldExample ------>是程序真正的入口 相当于 main函数

opt.source = tldInitSource(opt.source)---->视频数据输入 

输入的opt.source = struct('camera',0,'input','_input/','bb0',[])

输出是 又加了 idx 和 files属性 

其中idx是一个数组

files 是一个dir()函数的返回值 本质也是个数组 只是name 属性改成了完整的相对路径

如果要使用detector 检测 第一帧的 目标 可以在这个函数里 加。

